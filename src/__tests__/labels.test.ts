/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { beforeAll, expect, test } from "vitest";
import { Dwolla } from "../index.js";
import { createTestHTTPClient } from "./testclient.js";

let labelId: string | undefined;

beforeAll(async () => {

  const dwolla = new Dwolla({
    serverURL: "https://api-sandbox.dwolla.com",
    httpClient: createTestHTTPClient("labels"),
    security: {
      clientID: process.env["DWOLLA_CLIENT_ID"] ?? "value",
      clientSecret: process.env["DWOLLA_CLIENT_SECRET"] ?? "value",
    }
  });

  const created = await dwolla.customers.labels.create({
    id: "86d3ca4b-8c3c-4ba9-88fc-3be8243daf9d",
    requestBody: {
      amount: {
        value: "10.00",
        currency: "USD",
      },
    },
  });
  const locationHeader =
    created?.headers["Location"]?.[0] ?? created?.headers["location"]?.[0];
  expect(locationHeader).toBeDefined();
  labelId = new URL(locationHeader as string).pathname.split("/").pop();

});

test("Labels Get Label", async () => {
  const testHttpClient = createTestHTTPClient("getLabel");

  const dwolla = new Dwolla({
    serverURL: "https://api-sandbox.dwolla.com",
    httpClient: testHttpClient,
    security: {
      clientID: process.env["DWOLLA_CLIENT_ID"] ?? "value",
      clientSecret: process.env["DWOLLA_CLIENT_SECRET"] ?? "value",
    },
  });

  const result = await dwolla.labels.get({
    id: labelId!,
  });
  expect(result).toBeDefined();
  expect(result.id).toBeDefined();
  expect(result.created).toBeInstanceOf(Date);
  expect(result.amount).toBeDefined();
  expect(result.amount?.value).toBeDefined();
  expect(result.amount?.currency).toBe("USD");
  expect(result.links).toBeDefined();
  expect(result.links?.["self"]).toBeDefined();
});

test("Labels Remove Label", async () => {
  const testHttpClient = createTestHTTPClient("removeLabel");

  const dwolla = new Dwolla({
    serverURL: "https://api-sandbox.dwolla.com",
    httpClient: testHttpClient,
    security: {
      clientID: process.env["DWOLLA_CLIENT_ID"] ?? "value",
      clientSecret: process.env["DWOLLA_CLIENT_SECRET"] ?? "value",
    },
  });

  // create a label ledger entry to zero out the label to prepare for removal
  await dwolla.labels.ledgerEntries.create({
    id: labelId!,
    requestBody: {
      amount: {
        value: "-10.00",
        currency: "USD",
      },
    },
  });

  // remove the label
  const result = await dwolla.labels.remove({
      id: labelId!,
  });
  expect(result).toBeDefined();
  // Assert structure rather than exact payload
  expect(result.id).toBeDefined();
  expect(result.created).toBeInstanceOf(Date);
  expect(result.amount).toBeDefined();
  expect(result.amount?.value).toBeDefined();
  expect(result.amount?.currency).toBeDefined();
  expect(result.links).toBeDefined();
  expect(result.links?.["self"]).toBeDefined();
});
