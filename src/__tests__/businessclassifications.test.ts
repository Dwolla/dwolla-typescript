/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { expect, test } from "vitest";
import { Dwolla } from "../index.js";
import { createTestHTTPClient } from "./testclient.js";

test("Businessclassifications List Business Classifications", async () => {
  const testHttpClient = createTestHTTPClient("listBusinessClassifications");

  const dwolla = new Dwolla({
    serverURL: "https://api-sandbox.dwolla.com",
    httpClient: testHttpClient,
    security: {
      clientID: process.env["DWOLLA_CLIENT_ID"] ?? "value",
      clientSecret: process.env["DWOLLA_CLIENT_SECRET"] ?? "value",
    },
  });

  const result = await dwolla.businessClassifications.list();
  expect(result).toBeDefined();
  // Assert structure rather than exact payload to tolerate real sandbox data
  expect(result.links).toBeDefined();
  expect(result.embedded).toBeDefined();
  expect(Array.isArray(result.embedded?.businessClassifications ?? [])).toBe(
    true,
  );
  if ((result.embedded?.businessClassifications?.length ?? 0) > 0) {
    const first = result.embedded!.businessClassifications![0]!;
    expect(first.id).toBeDefined();
    expect(first.name).toBeDefined();
  }
});

test("Businessclassifications Retrieve Business Classification", async () => {
  const testHttpClient = createTestHTTPClient("retrieveBusinessClassification");

  const dwolla = new Dwolla({
    serverURL: "https://api-sandbox.dwolla.com",
    httpClient: testHttpClient,
    security: {
      clientID: process.env["DWOLLA_CLIENT_ID"] ?? "value",
      clientSecret: process.env["DWOLLA_CLIENT_SECRET"] ?? "value",
    },
  });

  const result = await dwolla.businessClassifications.get({
    id: "9ed3f669-7d6f-11e3-b545-5404a6144203",
  });
  expect(result).toBeDefined();
  // Assert structure rather than exact payload to tolerate real sandbox data
  expect(result.links).toBeDefined();
  expect(result.embedded).toBeDefined();
  expect(Array.isArray(result.embedded?.industryClassifications ?? [])).toBe(
    true,
  );
  expect(result.id).toBeDefined();
  expect(result.name).toBeDefined();
  if ((result.embedded?.industryClassifications?.length ?? 0) > 0) {
    const first = result.embedded!.industryClassifications![0]!;
    expect(first.id).toBeDefined();
    expect(first.name).toBeDefined();
  }
});
