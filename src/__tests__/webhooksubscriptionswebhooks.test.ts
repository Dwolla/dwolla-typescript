/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { expect, test } from "vitest";
import { Dwolla } from "../index.js";
import { createTestHTTPClient } from "./testclient.js";

test("Webhooksubscriptions Webhooks List Webhooks", async () => {
  const testHttpClient = createTestHTTPClient("listWebhooks");

  const dwolla = new Dwolla({
    serverURL: "https://api-sandbox.dwolla.com",
    httpClient: testHttpClient,
    security: {
      clientID: process.env["DWOLLA_CLIENT_ID"] ?? "value",
      clientSecret: process.env["DWOLLA_CLIENT_SECRET"] ?? "value",
    },
  });

  const result = await dwolla.webhookSubscriptions.webhooks.list({
    id: "ff29dec3-344d-4132-b890-151bd372ca0a",
  });
  expect(result).toBeDefined();
  // Assert structure rather than exact payload to tolerate real sandbox data
  expect(result.links).toBeDefined();
  expect(result.links?.self).toBeDefined();
  expect(result.embedded).toBeDefined();
  expect(Array.isArray(result.embedded?.webhooks ?? [])).toBe(true);

  if ((result.embedded?.webhooks?.length ?? 0) > 0) {
    const first = result.embedded!.webhooks![0]!;
    // Core webhook fields
    expect(first.id).toBeDefined();
    expect(first.topic).toBeDefined();
    expect(first.accountId).toBeDefined();
    expect(first.eventId).toBeDefined();
    expect(first.subscriptionId).toBeDefined();

    // Links structure
    expect(first.links).toBeDefined();
    expect(first.links?.self).toBeDefined();
    expect(first.links?.subscription).toBeDefined();
    expect(first.links?.retry).toBeDefined();
    expect(first.links?.event).toBeDefined();

    // Attempts structure
    expect(Array.isArray(first.attempts ?? [])).toBe(true);
    if ((first.attempts?.length ?? 0) > 0) {
      const attempt = first.attempts![0]!;
      expect(attempt.id).toBeDefined();
      expect(attempt.request).toBeDefined();
      expect(attempt.request?.url).toBeDefined();
      expect(attempt.request?.timestamp).toBeInstanceOf(Date);
      expect(Array.isArray(attempt.request?.headers ?? [])).toBe(true);
      expect(attempt.response).toBeDefined();
      expect(typeof attempt.response?.statusCode).toBe("number");
      expect(attempt.response?.timestamp).toBeInstanceOf(Date);
    }
  }
});
