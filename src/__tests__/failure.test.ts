/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { expect, test } from "vitest";
import { Dwolla } from "../index.js";
import { createTestHTTPClient } from "./testclient.js";

test("Failure Get Transfer Failure Reason", async () => {
  const testHttpClient = createTestHTTPClient("getTransferFailureReason");

  const dwolla = new Dwolla({
    serverURL: "https://api-sandbox.dwolla.com",
    httpClient: testHttpClient,
    security: {
      clientID: process.env["DWOLLA_CLIENT_ID"] ?? "value",
      clientSecret: process.env["DWOLLA_CLIENT_SECRET"] ?? "value",
    },
  });

  const result = await dwolla.transfers.failure.get({
    id: "a90dfd45-99cf-f011-acd5-02ab38c54207",
  });
  expect(result).toBeDefined();
  // Assert structure rather than exact payload
  expect(result.code).toBeDefined();
  expect(result.description).toBeDefined();
  expect(result.links).toBeDefined();
  
  // Optional fields
  if (result.explanation) {
    expect(typeof result.explanation).toBe("string");
  }
});
