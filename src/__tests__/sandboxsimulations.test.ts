/*
 * Code originally generated by Speakeasy (https://www.speakeasy.com).
 */

import { beforeAll, expect, test } from "vitest";
import { Dwolla } from "../index.js";
import { createDwollaClient } from "./setup.js";

let dwolla: Dwolla;

beforeAll(async () => {
  dwolla = createDwollaClient("sandboxsimulations");
}, 30000);

test("Sandbox Simulations Simulate Bank Transfer Processing", async () => {
  const result = await dwolla.sandboxSimulations.simulate();
  expect(result).toBeDefined();
  // Sandbox simulation returns an empty object on success
});
