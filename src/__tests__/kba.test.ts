/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { afterAll, beforeAll, expect, test } from "vitest";
import { Dwolla } from "../index.js";
import { createTestHTTPClient } from "./testclient.js";

let kbaSessionId: string | undefined;
let customerId: string | undefined;

beforeAll(async () => {
  const dwolla = new Dwolla({
    serverURL: "https://api-sandbox.dwolla.com",
    httpClient: createTestHTTPClient("kba"),
    security: {
      clientID: process.env["DWOLLA_CLIENT_ID"] ?? "value",
      clientSecret: process.env["DWOLLA_CLIENT_SECRET"] ?? "value",
    },
  });

  // Create a Personal Verified Customer with firstName "kba" to trigger KBA status
  const timestamp = Date.now();
  const customerResult = await dwolla.customers.create({
    firstName: "kba",
    lastName: "Test",
    email: `kba-test-${timestamp}@example.com`,
    type: "personal",
    address1: "123 Main St",
    city: "Des Moines",
    state: "IA",
    postalCode: "50309",
    dateOfBirth: "1980-01-01",
    ssn: "1234",
  });

  const customerLocationHeader =
    customerResult?.headers["Location"]?.[0] ?? customerResult?.headers["location"]?.[0];
  expect(customerLocationHeader).toBeDefined();
  customerId = new URL(customerLocationHeader as string).pathname.split("/").pop();

  // Initiate a KBA session for the customer
  const kbaResult = await dwolla.customers.kba.initiate({
    id: customerId!,
  });

  const kbaLocationHeader =
    kbaResult?.headers["Location"]?.[0] ?? kbaResult?.headers["location"]?.[0];
  expect(kbaLocationHeader).toBeDefined();
  kbaSessionId = new URL(kbaLocationHeader as string).pathname.split("/").pop();
});

afterAll(async () => {
  // Clean up: deactivate the customer
  if (customerId) {
    const dwolla = new Dwolla({
      serverURL: "https://api-sandbox.dwolla.com",
      httpClient: createTestHTTPClient("kba-cleanup"),
      security: {
        clientID: process.env["DWOLLA_CLIENT_ID"] ?? "value",
        clientSecret: process.env["DWOLLA_CLIENT_SECRET"] ?? "value",
      },
    });

    await dwolla.customers.update({
      id: customerId,
      requestBody: {
        status: "deactivated",
      },
    });
  }
});

test("Kba Get Kba Questions", async () => {
  const testHttpClient = createTestHTTPClient("getKbaQuestions");

  const dwolla = new Dwolla({
    serverURL: "https://api-sandbox.dwolla.com",
    httpClient: testHttpClient,
    security: {
      clientID: process.env["DWOLLA_CLIENT_ID"] ?? "value",
      clientSecret: process.env["DWOLLA_CLIENT_SECRET"] ?? "value",
    },
  });

  const result = await dwolla.kba.getQuestions({
    id: kbaSessionId!,
  });
  expect(result).toBeDefined();
  // Assert structure rather than exact payload
  expect(result.id).toBeDefined();
  expect(result.links).toBeDefined();
  expect(result.links?.answer).toBeDefined();
  expect(Array.isArray(result.questions ?? [])).toBe(true);
  
  // Validate shape of the first question if present
  if ((result.questions?.length ?? 0) > 0) {
    const first = result.questions![0]!;
    expect(first.id).toBeDefined();
    expect(first.text).toBeDefined();
    expect(Array.isArray(first.answers ?? [])).toBe(true);
    
    if ((first.answers?.length ?? 0) > 0) {
      const firstAnswer = first.answers![0]!;
      expect(firstAnswer.id).toBeDefined();
      expect(firstAnswer.text).toBeDefined();
    }
  }
});

test("Kba Verify Kba Questions", async () => {
  const testHttpClient = createTestHTTPClient("verifyKbaQuestions");

  const dwolla = new Dwolla({
    serverURL: "https://api-sandbox.dwolla.com",
    httpClient: testHttpClient,
    security: {
      clientID: process.env["DWOLLA_CLIENT_ID"] ?? "value",
      clientSecret: process.env["DWOLLA_CLIENT_SECRET"] ?? "value",
    },
  });

  const result = await dwolla.kba.verify({
    id: kbaSessionId!,
    requestBody: 
      {
        "answers": [
            {
                "questionId": "2355953375",
                "answerId": "2687969335"
            },
            {
                "questionId": "2355953385",
                "answerId": "2687969385"
            },
            {
                "questionId": "2355953395",
                "answerId": "2687969435"
            },
            {
                "questionId": "2355953405",
                "answerId": "2687969485"
            }
        ]
    },
  });
  expect(result).toBeDefined();
  // Assert structure rather than exact payload
  expect(result.verificationStatus).toBeDefined();
  expect(result.links).toBeDefined();
  expect(result.links?.customer).toBeDefined();
});
