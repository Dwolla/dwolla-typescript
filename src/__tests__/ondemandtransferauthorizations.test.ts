/*
 * Code originally generated by Speakeasy (https://www.speakeasy.com).
 */

import { beforeAll, expect, test } from "vitest";
import { Dwolla } from "../index.js";
import { createDwollaClient } from "./setup.js";

let dwolla: Dwolla;

beforeAll(async () => {
  dwolla = createDwollaClient("ondemandtransferauthorizations");
}, 30000);

test("Ondemandtransferauthorizations Create On Demand Transfer Authorization", async () => {
  const result = await dwolla.fundingSources.onDemandTransferAuthorizations.create();
  expect(result).toBeDefined();
  expect(result.links).toBeDefined();
  expect(result.links?.self).toBeDefined();
  expect(result.bodyText).toBeDefined();
  expect(result.buttonText).toBeDefined();
});
