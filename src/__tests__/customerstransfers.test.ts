/*
 * Code originally generated by Speakeasy (https://www.speakeasy.com).
 */

import { expect, test } from "vitest";
import { Dwolla } from "../index.js";
import { createTestHTTPClient } from "./testclient.js";

test("Customers Transfers List Customer Transfers", async () => {
  const testHttpClient = createTestHTTPClient("listCustomerTransfers");

  const dwolla = new Dwolla({
    serverURL: "https://api-sandbox.dwolla.com",
    httpClient: testHttpClient,
    security: {
      clientID: process.env["DWOLLA_CLIENT_ID"] ?? "value",
      clientSecret: process.env["DWOLLA_CLIENT_SECRET"] ?? "value",
    },
  });

  const result = await dwolla.customers.transfers.list({
    id: "86d3ca4b-8c3c-4ba9-88fc-3be8243daf9d",
  });
  expect(result).toBeDefined();
  // Assert structure rather than exact payload
  expect(result.links).toBeDefined();
  expect(result.links?.["self"]).toBeDefined();
  expect(result.embedded).toBeDefined();
  expect(Array.isArray(result.embedded?.transfers ?? [])).toBe(true);
  expect(typeof result.total).toBe("number");

  // Validate shape of the first transfer if present
  if ((result.embedded?.transfers?.length ?? 0) > 0) {
    const first = result.embedded!.transfers![0]!;
    expect(first.id).toBeDefined();
    expect(first.status).toBeDefined();
    expect(first.created).toBeInstanceOf(Date);
    expect(first.amount).toBeDefined();
    expect(first.amount?.value).toBeDefined();
    expect(first.amount?.currency).toBeDefined();
    expect(first.links).toBeDefined();
    expect(first.links?.["self"]).toBeDefined();
    
    // Optional fields
    if (first.clearing) {
      expect(first.clearing.source).toBeDefined();
    }
    if (first.correlationId) {
      expect(typeof first.correlationId).toBe("string");
    }
    if (first.metadata) {
      expect(typeof first.metadata).toBe("object");
    }
    if (first.achDetails) {
      expect(typeof first.achDetails).toBe("object");
    }
    if (first.rtpDetails) {
      expect(typeof first.rtpDetails).toBe("object");
    }
    if (first.processingChannel) {
      expect(typeof first.processingChannel).toBe("object");
    }
  }
});
