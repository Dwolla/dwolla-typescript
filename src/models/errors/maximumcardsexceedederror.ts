/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import * as models from "../index.js";
import { DwollaError } from "./dwollaerror.js";

/**
 * Error returned when a customer has exceeded the maximum number of debit card funding sources
 */
export type MaximumCardsExceededErrorData = {
  code: string;
  message: string;
  embedded: models.MaximumCardsExceededErrorEmbedded;
};

/**
 * Error returned when a customer has exceeded the maximum number of debit card funding sources
 */
export class MaximumCardsExceededError extends DwollaError {
  code: string;
  embedded: models.MaximumCardsExceededErrorEmbedded;

  /** The original data that was passed to this error instance. */
  data$: MaximumCardsExceededErrorData;

  constructor(
    err: MaximumCardsExceededErrorData,
    httpMeta: { response: Response; request: Request; body: string },
  ) {
    const message = err.message || `API error occurred: ${JSON.stringify(err)}`;
    super(message, httpMeta);
    this.data$ = err;
    this.code = err.code;
    this.embedded = err.embedded;

    this.name = "MaximumCardsExceededError";
  }
}

/** @internal */
export const MaximumCardsExceededError$inboundSchema: z.ZodType<
  MaximumCardsExceededError,
  z.ZodTypeDef,
  unknown
> = z.object({
  code: z.string(),
  message: z.string(),
  _embedded: z.lazy(() =>
    models.MaximumCardsExceededErrorEmbedded$inboundSchema
  ),
  request$: z.instanceof(Request),
  response$: z.instanceof(Response),
  body$: z.string(),
})
  .transform((v) => {
    const remapped = remap$(v, {
      "_embedded": "embedded",
    });

    return new MaximumCardsExceededError(remapped, {
      request: v.request$,
      response: v.response$,
      body: v.body$,
    });
  });
