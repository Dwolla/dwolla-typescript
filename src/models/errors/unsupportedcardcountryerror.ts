/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import * as models from "../index.js";
import { DwollaError } from "./dwollaerror.js";

/**
 * Error returned when attempting to create a debit card funding source from an unsupported country
 */
export type UnsupportedCardCountryErrorData = {
  code: string;
  message: string;
  embedded: models.UnsupportedCardCountryErrorEmbedded;
};

/**
 * Error returned when attempting to create a debit card funding source from an unsupported country
 */
export class UnsupportedCardCountryError extends DwollaError {
  code: string;
  embedded: models.UnsupportedCardCountryErrorEmbedded;

  /** The original data that was passed to this error instance. */
  data$: UnsupportedCardCountryErrorData;

  constructor(
    err: UnsupportedCardCountryErrorData,
    httpMeta: { response: Response; request: Request; body: string },
  ) {
    const message = err.message || `API error occurred: ${JSON.stringify(err)}`;
    super(message, httpMeta);
    this.data$ = err;
    this.code = err.code;
    this.embedded = err.embedded;

    this.name = "UnsupportedCardCountryError";
  }
}

/** @internal */
export const UnsupportedCardCountryError$inboundSchema: z.ZodType<
  UnsupportedCardCountryError,
  z.ZodTypeDef,
  unknown
> = z.object({
  code: z.string(),
  message: z.string(),
  _embedded: z.lazy(() =>
    models.UnsupportedCardCountryErrorEmbedded$inboundSchema
  ),
  request$: z.instanceof(Request),
  response$: z.instanceof(Response),
  body$: z.string(),
})
  .transform((v) => {
    const remapped = remap$(v, {
      "_embedded": "embedded",
    });

    return new UnsupportedCardCountryError(remapped, {
      request: v.request$,
      response: v.response$,
      body: v.body$,
    });
  });
