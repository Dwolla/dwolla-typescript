/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { massPaymentsUpdateMassPayment } from "../../funcs/massPaymentsUpdateMassPayment.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.UpdateMassPaymentRequest$inboundSchema,
};

export const tool$massPaymentsUpdateMassPayment: ToolDefinition<typeof args> = {
  name: "mass-payments-update-mass-payment",
  description: `Update a mass payment to process or cancel it

Update a mass payment to process or cancel it`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await massPaymentsUpdateMassPayment(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
