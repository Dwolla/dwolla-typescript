/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { transfersCancelTransfer } from "../../funcs/transfersCancelTransfer.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.CancelTransferRequest$inboundSchema,
};

export const tool$transfersCancelTransfer: ToolDefinition<typeof args> = {
  name: "transfers-cancel-transfer",
  description: `Cancel a transfer

Cancel a transfer`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await transfersCancelTransfer(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
