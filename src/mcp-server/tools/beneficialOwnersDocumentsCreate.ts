/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { beneficialOwnersDocumentsCreate } from "../../funcs/beneficialOwnersDocumentsCreate.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.CreateBeneficialOwnerDocumentRequest$inboundSchema,
};

export const tool$beneficialOwnersDocumentsCreate: ToolDefinition<typeof args> =
  {
    name: "beneficial-owners-documents-create",
    description: `Create a document for beneficial owner

Create a document for beneficial owner`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await beneficialOwnersDocumentsCreate(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      return formatResult(void 0, apiCall);
    },
  };
