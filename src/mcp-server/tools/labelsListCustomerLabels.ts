/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { labelsListCustomerLabels } from "../../funcs/labelsListCustomerLabels.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListCustomerLabelsRequest$inboundSchema,
};

export const tool$labelsListCustomerLabels: ToolDefinition<typeof args> = {
  name: "labels-list-customer-labels",
  description: `List labels for a customer

List labels for a customer`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await labelsListCustomerLabels(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
