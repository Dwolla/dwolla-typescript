/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { massPaymentsGetMassPayment } from "../../funcs/massPaymentsGetMassPayment.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetMassPaymentRequest$inboundSchema,
};

export const tool$massPaymentsGetMassPayment: ToolDefinition<typeof args> = {
  name: "mass-payments-get-mass-payment",
  description: `Retrieve a mass payment

Retrieve a mass payment`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await massPaymentsGetMassPayment(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
