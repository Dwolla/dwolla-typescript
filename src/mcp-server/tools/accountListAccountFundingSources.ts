/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { accountListAccountFundingSources } from "../../funcs/accountListAccountFundingSources.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListAccountFundingSourcesRequest$inboundSchema,
};

export const tool$accountListAccountFundingSources: ToolDefinition<
  typeof args
> = {
  name: "account-list-account-funding-sources",
  description: `List funding sources for an account

List funding source for an account`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await accountListAccountFundingSources(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
