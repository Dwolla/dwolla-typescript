/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { fundingSourcesCreateCustomerFundingSourceToken } from "../../funcs/fundingSourcesCreateCustomerFundingSourceToken.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.CreateCustomerFundingSourceTokenRequest$inboundSchema,
};

export const tool$fundingSourcesCreateCustomerFundingSourceToken:
  ToolDefinition<typeof args> = {
    name: "funding-sources-create-customer-funding-source-token",
    description: `Create a funding sources token for dwolla.js

Create a funding sources token for dwolla.js`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] =
        await fundingSourcesCreateCustomerFundingSourceToken(
          client,
          args.request,
          { fetchOptions: { signal: ctx.signal } },
        ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };
