/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { fundingSourcesGetFundingSource } from "../../funcs/fundingSourcesGetFundingSource.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetFundingSourceRequest$inboundSchema,
};

export const tool$fundingSourcesGetFundingSource: ToolDefinition<typeof args> =
  {
    name: "funding-sources-get-funding-source",
    description: `Retrieve a funding source

Retrieve a funding source`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await fundingSourcesGetFundingSource(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };
