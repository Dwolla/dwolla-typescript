/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { accountGetAccount } from "../../funcs/accountGetAccount.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetAccountRequest$inboundSchema,
};

export const tool$accountGetAccount: ToolDefinition<typeof args> = {
  name: "account-get-account",
  description: `Retrieve Account Details

Retrieve basic account information belonging to the authorized Dwolla account.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await accountGetAccount(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
