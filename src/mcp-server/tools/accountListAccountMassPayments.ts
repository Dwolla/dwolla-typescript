/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { accountListAccountMassPayments } from "../../funcs/accountListAccountMassPayments.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListAccountMassPaymentsRequest$inboundSchema,
};

export const tool$accountListAccountMassPayments: ToolDefinition<typeof args> =
  {
    name: "account-list-account-mass-payments",
    description: `List mass payments for an account

List mass payments for an account`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await accountListAccountMassPayments(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };
