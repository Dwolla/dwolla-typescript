/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { beneficialOwnersCreateBeneficialOwnerForCustomer } from "../../funcs/beneficialOwnersCreateBeneficialOwnerForCustomer.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.CreateBeneficialOwnerForCustomerRequest$inboundSchema,
};

export const tool$beneficialOwnersCreateBeneficialOwnerForCustomer:
  ToolDefinition<typeof args> = {
    name: "beneficial-owners-create-beneficial-owner-for-customer",
    description: `Create beneficial owner

Create beneficial owner which is a natural person who, directly or indirectly, owns 25% or more of the equity interests of the company.`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] =
        await beneficialOwnersCreateBeneficialOwnerForCustomer(
          client,
          args.request,
          { fetchOptions: { signal: ctx.signal } },
        ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      return formatResult(void 0, apiCall);
    },
  };
