/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { kbaVerifyKbaQuestions } from "../../funcs/kbaVerifyKbaQuestions.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.VerifyKbaQuestionsRequest$inboundSchema,
};

export const tool$kbaVerifyKbaQuestions: ToolDefinition<typeof args> = {
  name: "kba-verify-kba-questions",
  description: `Verify KBA Questions

Verify KBA Questions`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await kbaVerifyKbaQuestions(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
