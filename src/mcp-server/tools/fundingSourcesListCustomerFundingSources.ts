/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { fundingSourcesListCustomerFundingSources } from "../../funcs/fundingSourcesListCustomerFundingSources.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListCustomerFundingSourcesRequest$inboundSchema,
};

export const tool$fundingSourcesListCustomerFundingSources: ToolDefinition<
  typeof args
> = {
  name: "funding-sources-list-customer-funding-sources",
  description: `List customer funding sources

List customer funding sources`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await fundingSourcesListCustomerFundingSources(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
