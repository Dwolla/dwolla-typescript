/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { exchangesGetExchangePartner } from "../../funcs/exchangesGetExchangePartner.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetExchangePartnerRequest$inboundSchema,
};

export const tool$exchangesGetExchangePartner: ToolDefinition<typeof args> = {
  name: "exchanges-get-exchange-partner",
  description: `Retrieve exchange partner

Retrieve exchange partner`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await exchangesGetExchangePartner(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
