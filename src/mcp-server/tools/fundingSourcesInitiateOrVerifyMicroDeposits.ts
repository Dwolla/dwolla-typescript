/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { fundingSourcesInitiateOrVerifyMicroDeposits } from "../../funcs/fundingSourcesInitiateOrVerifyMicroDeposits.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.InitiateOrVerifyMicroDepositsRequest$inboundSchema,
};

export const tool$fundingSourcesInitiateOrVerifyMicroDeposits: ToolDefinition<
  typeof args
> = {
  name: "funding-sources-initiate-or-verify-micro-deposits",
  description: `Initiate or Verify micro-deposits

Initiate or Verify micro-deposits.
For initiating micro-deposits, no request body is required.
For verifying micro-deposits, a request body with the micro-deposit amounts is required.
`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await fundingSourcesInitiateOrVerifyMicroDeposits(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
