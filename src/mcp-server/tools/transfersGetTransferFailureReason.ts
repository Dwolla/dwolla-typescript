/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { transfersGetTransferFailureReason } from "../../funcs/transfersGetTransferFailureReason.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetTransferFailureReasonRequest$inboundSchema,
};

export const tool$transfersGetTransferFailureReason: ToolDefinition<
  typeof args
> = {
  name: "transfers-get-transfer-failure-reason",
  description: `Retrieve a transfer failure reason

Retrieve a transfer failure reason`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await transfersGetTransferFailureReason(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
