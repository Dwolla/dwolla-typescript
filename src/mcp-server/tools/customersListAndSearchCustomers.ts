/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { customersListAndSearchCustomers } from "../../funcs/customersListAndSearchCustomers.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListAndSearchCustomersRequest$inboundSchema,
};

export const tool$customersListAndSearchCustomers: ToolDefinition<typeof args> =
  {
    name: "customers-list-and-search-customers",
    description: `List and search customers

List and search customers allowing you to filter by email and status, as well as search on key fields such as firstName, lastName, and businessName.`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await customersListAndSearchCustomers(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };
