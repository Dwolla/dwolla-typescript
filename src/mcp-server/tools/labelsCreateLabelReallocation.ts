/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { labelsCreateLabelReallocation } from "../../funcs/labelsCreateLabelReallocation.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.CreateLabelReallocationRequest$inboundSchema,
};

export const tool$labelsCreateLabelReallocation: ToolDefinition<typeof args> = {
  name: "labels-create-label-reallocation",
  description: `Create a label reallocation

Create a label reallocation`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await labelsCreateLabelReallocation(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
