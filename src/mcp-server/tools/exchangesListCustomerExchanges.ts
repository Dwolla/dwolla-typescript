/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { exchangesListCustomerExchanges } from "../../funcs/exchangesListCustomerExchanges.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListCustomerExchangesRequest$inboundSchema,
};

export const tool$exchangesListCustomerExchanges: ToolDefinition<typeof args> =
  {
    name: "exchanges-list-customer-exchanges",
    description: `List exchanges for a customer

List exchanges for a customer`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await exchangesListCustomerExchanges(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };
