/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { fundingSourcesCreateCustomerFundingSource } from "../../funcs/fundingSourcesCreateCustomerFundingSource.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.CreateCustomerFundingSourceRequest$inboundSchema,
};

export const tool$fundingSourcesCreateCustomerFundingSource: ToolDefinition<
  typeof args
> = {
  name: "funding-sources-create-customer-funding-source",
  description: `Create customer funding source

Create a new funding source for a customer.
There are multiple methods available:
- Basic bank funding source (with routing/account numbers)
- Using an exchange
`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await fundingSourcesCreateCustomerFundingSource(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
