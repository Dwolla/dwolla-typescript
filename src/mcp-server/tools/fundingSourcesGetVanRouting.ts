/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { fundingSourcesGetVanRouting } from "../../funcs/fundingSourcesGetVanRouting.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetVanRoutingRequest$inboundSchema,
};

export const tool$fundingSourcesGetVanRouting: ToolDefinition<typeof args> = {
  name: "funding-sources-get-van-routing",
  description: `Retrieve VAN account and routing numbers

Retrieve account and routing numbers for a VAN (Virtual Account Number)`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await fundingSourcesGetVanRouting(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
