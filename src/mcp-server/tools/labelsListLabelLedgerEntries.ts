/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { labelsListLabelLedgerEntries } from "../../funcs/labelsListLabelLedgerEntries.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListLabelLedgerEntriesRequest$inboundSchema,
};

export const tool$labelsListLabelLedgerEntries: ToolDefinition<typeof args> = {
  name: "labels-list-label-ledger-entries",
  description: `List label ledger entries

List label ledger entries`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await labelsListLabelLedgerEntries(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
