/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { accountListAndSearchAccountTransfers } from "../../funcs/accountListAndSearchAccountTransfers.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListAndSearchAccountTransfersRequest$inboundSchema,
};

export const tool$accountListAndSearchAccountTransfers: ToolDefinition<
  typeof args
> = {
  name: "account-list-and-search-account-transfers",
  description: `List and search transfers for an account

List and search transfers for an account`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await accountListAndSearchAccountTransfers(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
