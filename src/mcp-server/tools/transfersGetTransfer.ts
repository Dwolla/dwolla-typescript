/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { transfersGetTransfer } from "../../funcs/transfersGetTransfer.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetTransferRequest$inboundSchema,
};

export const tool$transfersGetTransfer: ToolDefinition<typeof args> = {
  name: "transfers-get-transfer",
  description: `Retrieve a transfer

Retrieve a transfer`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await transfersGetTransfer(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
