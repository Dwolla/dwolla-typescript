/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { exchangeSessionsRetrieveCustomerExchangeSession } from "../../funcs/exchangeSessionsRetrieveCustomerExchangeSession.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.RetrieveCustomerExchangeSessionRequest$inboundSchema,
};

export const tool$exchangeSessionsRetrieveCustomerExchangeSession:
  ToolDefinition<typeof args> = {
    name: "exchange-sessions-retrieve-customer-exchange-session",
    description: `Retrieve exchange session

Retrieve details of an exchange session`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] =
        await exchangeSessionsRetrieveCustomerExchangeSession(
          client,
          args.request,
          { fetchOptions: { signal: ctx.signal } },
        ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };
