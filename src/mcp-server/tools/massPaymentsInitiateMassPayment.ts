/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { massPaymentsInitiateMassPayment } from "../../funcs/massPaymentsInitiateMassPayment.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.InitiateMassPaymentRequest$inboundSchema,
};

export const tool$massPaymentsInitiateMassPayment: ToolDefinition<typeof args> =
  {
    name: "mass-payments-initiate-mass-payment",
    description: `Initiate a mass payment

Initiate a mass payment`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await massPaymentsInitiateMassPayment(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      return formatResult(void 0, apiCall);
    },
  };
