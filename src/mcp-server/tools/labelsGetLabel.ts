/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { labelsGetLabel } from "../../funcs/labelsGetLabel.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetLabelRequest$inboundSchema,
};

export const tool$labelsGetLabel: ToolDefinition<typeof args> = {
  name: "labels-get-label",
  description: `Retrieve a label

Retrieve a label`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await labelsGetLabel(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
