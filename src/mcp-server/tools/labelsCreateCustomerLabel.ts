/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { labelsCreateCustomerLabel } from "../../funcs/labelsCreateCustomerLabel.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.CreateCustomerLabelRequest$inboundSchema,
};

export const tool$labelsCreateCustomerLabel: ToolDefinition<typeof args> = {
  name: "labels-create-customer-label",
  description: `Create a label for a customer

Create a label for a customer`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await labelsCreateCustomerLabel(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
