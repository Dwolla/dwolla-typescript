/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { kbaGetQuestions } from "../../funcs/kbaGetQuestions.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetKbaQuestionsRequest$inboundSchema,
};

export const tool$kbaGetQuestions: ToolDefinition<typeof args> = {
  name: "kba-get-questions",
  description: `Retrieve KBA Questions

Retrieve KBA Questions`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await kbaGetQuestions(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
