/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { webhooksListRetries } from "../../funcs/webhooksListRetries.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListWebhookRetriesRequest$inboundSchema,
};

export const tool$webhooksListRetries: ToolDefinition<typeof args> = {
  name: "webhooks-list-retries",
  description: `List retries for a webhook

List retries for a webhook`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await webhooksListRetries(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
