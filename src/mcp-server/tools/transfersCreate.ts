/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { transfersCreate } from "../../funcs/transfersCreate.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.InitiateTransferRequest$inboundSchema,
};

export const tool$transfersCreate: ToolDefinition<typeof args> = {
  name: "transfers-create",
  description: `Initiate a transfer

Initiate a transfer`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await transfersCreate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
