/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { beneficialOwnersRetrieveBeneficialOwner } from "../../funcs/beneficialOwnersRetrieveBeneficialOwner.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.RetrieveBeneficialOwnerRequest$inboundSchema,
};

export const tool$beneficialOwnersRetrieveBeneficialOwner: ToolDefinition<
  typeof args
> = {
  name: "beneficial-owners-retrieve-beneficial-owner",
  description: `Retrieve beneficial owner

Retrieve details for a single beneficial owner`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await beneficialOwnersRetrieveBeneficialOwner(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
