/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { webhookSubscriptionsGetWebhookSubscription } from "../../funcs/webhookSubscriptionsGetWebhookSubscription.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetWebhookSubscriptionRequest$inboundSchema,
};

export const tool$webhookSubscriptionsGetWebhookSubscription: ToolDefinition<
  typeof args
> = {
  name: "webhook-subscriptions-get-webhook-subscription",
  description: `Retrieve a webhook subscription

Retrieve a webhook subscription`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await webhookSubscriptionsGetWebhookSubscription(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
