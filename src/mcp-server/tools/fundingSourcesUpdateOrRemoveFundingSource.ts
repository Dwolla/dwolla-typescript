/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { fundingSourcesUpdateOrRemoveFundingSource } from "../../funcs/fundingSourcesUpdateOrRemoveFundingSource.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.UpdateOrRemoveFundingSourceRequest$inboundSchema,
};

export const tool$fundingSourcesUpdateOrRemoveFundingSource: ToolDefinition<
  typeof args
> = {
  name: "funding-sources-update-or-remove-funding-source",
  description: `Update or remove a funding source

Update or remove a funding source`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await fundingSourcesUpdateOrRemoveFundingSource(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
