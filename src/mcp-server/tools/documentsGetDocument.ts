/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { documentsGetDocument } from "../../funcs/documentsGetDocument.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetDocumentRequest$inboundSchema,
};

export const tool$documentsGetDocument: ToolDefinition<typeof args> = {
  name: "documents-get-document",
  description: `Retrieve a document

Retrieve a document for a customer or beneficial owner`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await documentsGetDocument(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
