/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { massPaymentsListCustomerMassPayments } from "../../funcs/massPaymentsListCustomerMassPayments.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListCustomerMassPaymentsRequest$inboundSchema,
};

export const tool$massPaymentsListCustomerMassPayments: ToolDefinition<
  typeof args
> = {
  name: "mass-payments-list-customer-mass-payments",
  description: `List mass payments for customer

List mass payments for customer`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await massPaymentsListCustomerMassPayments(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
