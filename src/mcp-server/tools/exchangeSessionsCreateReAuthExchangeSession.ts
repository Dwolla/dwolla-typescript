/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { exchangeSessionsCreateReAuthExchangeSession } from "../../funcs/exchangeSessionsCreateReAuthExchangeSession.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.CreateReAuthExchangeSessionRequest$inboundSchema,
};

export const tool$exchangeSessionsCreateReAuthExchangeSession: ToolDefinition<
  typeof args
> = {
  name: "exchange-sessions-create-re-auth-exchange-session",
  description: `Create re-authentication exchange session

Create a new re-authentication exchange session.
The request body is not required for Plaid Web sessions.
`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await exchangeSessionsCreateReAuthExchangeSession(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
