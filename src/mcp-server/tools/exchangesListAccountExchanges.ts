/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { exchangesListAccountExchanges } from "../../funcs/exchangesListAccountExchanges.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$exchangesListAccountExchanges: ToolDefinition = {
  name: "exchanges-list-account-exchanges",
  description: `List exchanges for an account

List exchanges for an account`,
  tool: async (client, ctx) => {
    const [result, apiCall] = await exchangesListAccountExchanges(
      client,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
