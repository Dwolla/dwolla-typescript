/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { transfersListForCustomer } from "../../funcs/transfersListForCustomer.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListCustomerTransfersRequest$inboundSchema,
};

export const tool$transfersListForCustomer: ToolDefinition<typeof args> = {
  name: "transfers-list-for-customer",
  description: `List and search transfers for a customer

List and search transfers for a customer`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await transfersListForCustomer(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
