/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { kbaInitiate } from "../../funcs/kbaInitiate.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.InitiateKbaForCustomerRequest$inboundSchema,
};

export const tool$kbaInitiate: ToolDefinition<typeof args> = {
  name: "kba-initiate",
  description: `Initiate a KBA session

Initiate a KBA session`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await kbaInitiate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
