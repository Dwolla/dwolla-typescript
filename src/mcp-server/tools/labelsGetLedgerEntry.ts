/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { labelsGetLedgerEntry } from "../../funcs/labelsGetLedgerEntry.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetLabelLedgerEntryRequest$inboundSchema,
};

export const tool$labelsGetLedgerEntry: ToolDefinition<typeof args> = {
  name: "labels-get-ledger-entry",
  description: `Retrieve a label ledger entry

Retrieve a label ledger entry`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await labelsGetLedgerEntry(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
