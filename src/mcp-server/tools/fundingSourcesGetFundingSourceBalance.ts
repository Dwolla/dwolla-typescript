/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { fundingSourcesGetFundingSourceBalance } from "../../funcs/fundingSourcesGetFundingSourceBalance.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetFundingSourceBalanceRequest$inboundSchema,
};

export const tool$fundingSourcesGetFundingSourceBalance: ToolDefinition<
  typeof args
> = {
  name: "funding-sources-get-funding-source-balance",
  description: `Retrieve funding source balance

Retrieve funding source balance`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await fundingSourcesGetFundingSourceBalance(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
