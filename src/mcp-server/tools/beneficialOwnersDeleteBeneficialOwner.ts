/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { beneficialOwnersDeleteBeneficialOwner } from "../../funcs/beneficialOwnersDeleteBeneficialOwner.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DeleteBeneficialOwnerRequest$inboundSchema,
};

export const tool$beneficialOwnersDeleteBeneficialOwner: ToolDefinition<
  typeof args
> = {
  name: "beneficial-owners-delete-beneficial-owner",
  description: `Delete beneficial owner

Delete beneficial owner`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await beneficialOwnersDeleteBeneficialOwner(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
