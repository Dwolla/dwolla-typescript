/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { accountCreateAccountFundingSource } from "../../funcs/accountCreateAccountFundingSource.js";
import * as models from "../../models/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: models.CreateFundingSourceForAccountRequestBody$inboundSchema,
};

export const tool$accountCreateAccountFundingSource: ToolDefinition<
  typeof args
> = {
  name: "account-create-account-funding-source",
  description: `Create a funding source for an account

Create a funding source for an account`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await accountCreateAccountFundingSource(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
