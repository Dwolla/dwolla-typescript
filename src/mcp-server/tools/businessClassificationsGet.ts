/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { businessClassificationsGet } from "../../funcs/businessClassificationsGet.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.RetrieveBusinessClassificationRequest$inboundSchema,
};

export const tool$businessClassificationsGet: ToolDefinition<typeof args> = {
  name: "business-classifications-get",
  description: `Retrieve a business classification

Retrieve a business classification`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await businessClassificationsGet(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
