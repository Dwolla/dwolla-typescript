/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { kbaVerify } from "../../funcs/kbaVerify.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.VerifyRequest$inboundSchema,
};

export const tool$kbaVerify: ToolDefinition<typeof args> = {
  name: "kba-verify",
  description: `Verify KBA Questions

Verify KBA Questions`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await kbaVerify(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
