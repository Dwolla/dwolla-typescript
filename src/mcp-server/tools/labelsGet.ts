/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { labelsGet } from "../../funcs/labelsGet.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetLabelRequest$inboundSchema,
};

export const tool$labelsGet: ToolDefinition<typeof args> = {
  name: "labels-get",
  description: `Retrieve a label

Retrieve a label`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await labelsGet(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
