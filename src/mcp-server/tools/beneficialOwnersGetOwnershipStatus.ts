/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { beneficialOwnersGetOwnershipStatus } from "../../funcs/beneficialOwnersGetOwnershipStatus.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request:
    operations.GetBeneficialOwnershipStatusForCustomerRequest$inboundSchema,
};

export const tool$beneficialOwnersGetOwnershipStatus: ToolDefinition<
  typeof args
> = {
  name: "beneficial-owners-get-ownership-status",
  description: `Retrieve beneficial ownership status

Retrieve beneficial ownership status`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await beneficialOwnersGetOwnershipStatus(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
