/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { transfersListFees } from "../../funcs/transfersListFees.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListTransferFeesRequest$inboundSchema,
};

export const tool$transfersListFees: ToolDefinition<typeof args> = {
  name: "transfers-list-fees",
  description: `List fees for a transfer

List fees for a transfer`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await transfersListFees(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
