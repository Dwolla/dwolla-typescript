/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { labelsCreateLabelLedgerEntry } from "../../funcs/labelsCreateLabelLedgerEntry.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.CreateLabelLedgerEntryRequest$inboundSchema,
};

export const tool$labelsCreateLabelLedgerEntry: ToolDefinition<typeof args> = {
  name: "labels-create-label-ledger-entry",
  description: `Create label ledger entries

Create label ledger entries`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await labelsCreateLabelLedgerEntry(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
