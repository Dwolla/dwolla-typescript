/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { transfersCreateOnDemandTransferAuthorization } from "../../funcs/transfersCreateOnDemandTransferAuthorization.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$transfersCreateOnDemandTransferAuthorization: ToolDefinition =
  {
    name: "transfers-create-on-demand-transfer-authorization",
    description: `Create an on-demand transfer authorization

Create an on-demand transfer authorization`,
    tool: async (client, ctx) => {
      const [result, apiCall] =
        await transfersCreateOnDemandTransferAuthorization(
          client,
          { fetchOptions: { signal: ctx.signal } },
        ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };
