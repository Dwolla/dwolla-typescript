/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { documentsListCustomerDocuments } from "../../funcs/documentsListCustomerDocuments.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListCustomerDocumentsRequest$inboundSchema,
};

export const tool$documentsListCustomerDocuments: ToolDefinition<typeof args> =
  {
    name: "documents-list-customer-documents",
    description: `List documents for customer

List documents for customer`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await documentsListCustomerDocuments(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };
