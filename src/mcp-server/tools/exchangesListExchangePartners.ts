/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { exchangesListExchangePartners } from "../../funcs/exchangesListExchangePartners.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$exchangesListExchangePartners: ToolDefinition = {
  name: "exchanges-list-exchange-partners",
  description: `List exchange partners

List exchange partners`,
  tool: async (client, ctx) => {
    const [result, apiCall] = await exchangesListExchangePartners(
      client,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
