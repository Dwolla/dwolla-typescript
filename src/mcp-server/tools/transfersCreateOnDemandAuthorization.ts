/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { transfersCreateOnDemandAuthorization } from "../../funcs/transfersCreateOnDemandAuthorization.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$transfersCreateOnDemandAuthorization: ToolDefinition = {
  name: "transfers-create-on-demand-authorization",
  description: `Create an on-demand transfer authorization

Create an on-demand transfer authorization`,
  tool: async (client, ctx) => {
    const [result, apiCall] = await transfersCreateOnDemandAuthorization(
      client,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
