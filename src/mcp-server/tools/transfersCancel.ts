/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { transfersCancel } from "../../funcs/transfersCancel.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.CancelTransferRequest$inboundSchema,
};

export const tool$transfersCancel: ToolDefinition<typeof args> = {
  name: "transfers-cancel",
  description: `Cancel a transfer

Cancel a transfer`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await transfersCancel(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
