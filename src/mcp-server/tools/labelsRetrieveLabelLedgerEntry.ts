/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { labelsRetrieveLabelLedgerEntry } from "../../funcs/labelsRetrieveLabelLedgerEntry.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.RetrieveLabelLedgerEntryRequest$inboundSchema,
};

export const tool$labelsRetrieveLabelLedgerEntry: ToolDefinition<typeof args> =
  {
    name: "labels-retrieve-label-ledger-entry",
    description: `Retrieve a label reallocation

Retrieve a label reallocation`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await labelsRetrieveLabelLedgerEntry(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };
