/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { fundingSourcesGetVanAchRouting } from "../../funcs/fundingSourcesGetVanAchRouting.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetVanAchRoutingRequest$inboundSchema,
};

export const tool$fundingSourcesGetVanAchRouting: ToolDefinition<typeof args> =
  {
    name: "funding-sources-get-van-ach-routing",
    description:
      `Retrieve account and routing numbers for a VAN (Virtual Account Number)

Retrieve account and routing numbers for a VAN (Virtual Account Number)`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await fundingSourcesGetVanAchRouting(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };
