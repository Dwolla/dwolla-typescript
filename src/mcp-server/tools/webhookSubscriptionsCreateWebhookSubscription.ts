/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { webhookSubscriptionsCreateWebhookSubscription } from "../../funcs/webhookSubscriptionsCreateWebhookSubscription.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.CreateWebhookSubscriptionRequest$inboundSchema,
};

export const tool$webhookSubscriptionsCreateWebhookSubscription: ToolDefinition<
  typeof args
> = {
  name: "webhook-subscriptions-create-webhook-subscription",
  description: `Create a webhook subscription

Create a webhook subscription`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] =
      await webhookSubscriptionsCreateWebhookSubscription(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
