/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { labelsCreateReallocation } from "../../funcs/labelsCreateReallocation.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.CreateLabelReallocationRequest$inboundSchema,
};

export const tool$labelsCreateReallocation: ToolDefinition<typeof args> = {
  name: "labels-create-reallocation",
  description: `Create a label reallocation

Create a label reallocation`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await labelsCreateReallocation(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
