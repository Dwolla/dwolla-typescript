/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { exchangePartnersGet } from "../../funcs/exchangePartnersGet.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetExchangePartnerRequest$inboundSchema,
};

export const tool$exchangePartnersGet: ToolDefinition<typeof args> = {
  name: "exchange-partners-get",
  description: `Retrieve exchange partner

Retrieve exchange partner`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await exchangePartnersGet(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
