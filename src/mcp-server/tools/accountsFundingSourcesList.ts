/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { accountsFundingSourcesList } from "../../funcs/accountsFundingSourcesList.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListFundingSourcesRequest$inboundSchema,
};

export const tool$accountsFundingSourcesList: ToolDefinition<typeof args> = {
  name: "accounts-funding-sources-list",
  description: `List funding sources for an account

List funding source for an account`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await accountsFundingSourcesList(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
