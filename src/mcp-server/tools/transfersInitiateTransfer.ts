/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { transfersInitiateTransfer } from "../../funcs/transfersInitiateTransfer.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.InitiateTransferRequest$inboundSchema,
};

export const tool$transfersInitiateTransfer: ToolDefinition<typeof args> = {
  name: "transfers-initiate-transfer",
  description: `Initiate a transfer

Initiate a transfer`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await transfersInitiateTransfer(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
