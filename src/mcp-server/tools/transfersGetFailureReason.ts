/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { transfersGetFailureReason } from "../../funcs/transfersGetFailureReason.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetTransferFailureReasonRequest$inboundSchema,
};

export const tool$transfersGetFailureReason: ToolDefinition<typeof args> = {
  name: "transfers-get-failure-reason",
  description: `Retrieve a transfer failure reason

Retrieve a transfer failure reason`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await transfersGetFailureReason(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
