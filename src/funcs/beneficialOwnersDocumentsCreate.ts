/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { DwollaCore } from "../core.js";
import { appendForm, encodeSimple } from "../lib/encodings.js";
import {
  getContentTypeFromFileName,
  readableStreamToArrayBuffer,
} from "../lib/files.js";
import * as M from "../lib/matchers.js";
import { compactMap } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { RequestOptions } from "../lib/sdks.js";
import { extractSecurity, resolveGlobalSecurity } from "../lib/security.js";
import { pathToFunc } from "../lib/url.js";
import { DwollaError } from "../models/errors/dwollaerror.js";
import {
  ConnectionError,
  InvalidRequestError,
  RequestAbortedError,
  RequestTimeoutError,
  UnexpectedClientError,
} from "../models/errors/httpclienterrors.js";
import * as errors from "../models/errors/index.js";
import { ResponseValidationError } from "../models/errors/responsevalidationerror.js";
import { SDKValidationError } from "../models/errors/sdkvalidationerror.js";
import * as operations from "../models/operations/index.js";
import { APICall, APIPromise } from "../types/async.js";
import { isBlobLike } from "../types/blobs.js";
import { Result } from "../types/fp.js";
import { isReadableStream } from "../types/streams.js";

/**
 * Create a document for beneficial owner
 *
 * @remarks
 * Uploads an identity verification document for a beneficial owner using multipart form-data. Required when a beneficial owner has "document" status during the business verification process.
 */
export function beneficialOwnersDocumentsCreate(
  client: DwollaCore,
  request: operations.CreateBeneficialOwnerDocumentRequest,
  options?: RequestOptions,
): APIPromise<
  Result<
    operations.CreateBeneficialOwnerDocumentResponse | undefined,
    | errors.CreateBeneficialOwnerDocumentDwollaV1HalJSON
    | errors.ForbiddenError
    | errors.NotFoundError
    | errors.CreateBeneficialOwnerDocumentDwollaV1HalJSONError
    | DwollaError
    | ResponseValidationError
    | ConnectionError
    | RequestAbortedError
    | RequestTimeoutError
    | InvalidRequestError
    | UnexpectedClientError
    | SDKValidationError
  >
> {
  return new APIPromise($do(
    client,
    request,
    options,
  ));
}

async function $do(
  client: DwollaCore,
  request: operations.CreateBeneficialOwnerDocumentRequest,
  options?: RequestOptions,
): Promise<
  [
    Result<
      operations.CreateBeneficialOwnerDocumentResponse | undefined,
      | errors.CreateBeneficialOwnerDocumentDwollaV1HalJSON
      | errors.ForbiddenError
      | errors.NotFoundError
      | errors.CreateBeneficialOwnerDocumentDwollaV1HalJSONError
      | DwollaError
      | ResponseValidationError
      | ConnectionError
      | RequestAbortedError
      | RequestTimeoutError
      | InvalidRequestError
      | UnexpectedClientError
      | SDKValidationError
    >,
    APICall,
  ]
> {
  const parsed = safeParse(
    request,
    (value) =>
      operations.CreateBeneficialOwnerDocumentRequest$outboundSchema.parse(
        value,
      ),
    "Input validation failed",
  );
  if (!parsed.ok) {
    return [parsed, { status: "invalid" }];
  }
  const payload = parsed.value;
  const body = new FormData();

  if (payload.RequestBody.documentType !== undefined) {
    appendForm(body, "documentType", payload.RequestBody.documentType);
  }
  if (payload.RequestBody.file !== undefined) {
    if (isBlobLike(payload.RequestBody.file)) {
      appendForm(body, "file", payload.RequestBody.file);
    } else if (isReadableStream(payload.RequestBody.file.content)) {
      const buffer = await readableStreamToArrayBuffer(
        payload.RequestBody.file.content,
      );
      const contentType =
        getContentTypeFromFileName(payload.RequestBody.file.fileName)
        || "application/octet-stream";
      const blob = new Blob([buffer], { type: contentType });
      appendForm(body, "file", blob, payload.RequestBody.file.fileName);
    } else {
      const contentType =
        getContentTypeFromFileName(payload.RequestBody.file.fileName)
        || "application/octet-stream";
      appendForm(
        body,
        "file",
        new Blob([payload.RequestBody.file.content], { type: contentType }),
        payload.RequestBody.file.fileName,
      );
    }
  }

  const pathParams = {
    id: encodeSimple("id", payload.id, {
      explode: false,
      charEncoding: "percent",
    }),
  };

  const path = pathToFunc("/beneficial-owners/{id}/documents")(pathParams);

  const headers = new Headers(compactMap({
    Accept: "application/vnd.dwolla.v1.hal+json",
  }));

  const securityInput = await extractSecurity(client._options.security);
  const requestSecurity = resolveGlobalSecurity(securityInput);

  const context = {
    options: client._options,
    baseURL: options?.serverURL ?? client._baseURL ?? "",
    operationID: "createBeneficialOwnerDocument",
    oAuth2Scopes: [],

    resolvedSecurity: requestSecurity,

    securitySource: client._options.security,
    retryConfig: options?.retries
      || client._options.retryConfig
      || { strategy: "none" },
    retryCodes: options?.retryCodes || ["429", "500", "502", "503", "504"],
  };

  const requestRes = client._createRequest(context, {
    security: requestSecurity,
    method: "POST",
    baseURL: options?.serverURL,
    path: path,
    headers: headers,
    body: body,
    userAgent: client._options.userAgent,
    timeoutMs: options?.timeoutMs || client._options.timeoutMs || -1,
  }, options);
  if (!requestRes.ok) {
    return [requestRes, { status: "invalid" }];
  }
  const req = requestRes.value;

  const doResult = await client._do(req, {
    context,
    errorCodes: ["400", "403", "404", "413", "4XX", "5XX"],
    retryConfig: context.retryConfig,
    retryCodes: context.retryCodes,
  });
  if (!doResult.ok) {
    return [doResult, { status: "request-error", request: req }];
  }
  const response = doResult.value;

  const responseFields = {
    HttpMeta: { Response: response, Request: req },
  };

  const [result] = await M.match<
    operations.CreateBeneficialOwnerDocumentResponse | undefined,
    | errors.CreateBeneficialOwnerDocumentDwollaV1HalJSON
    | errors.ForbiddenError
    | errors.NotFoundError
    | errors.CreateBeneficialOwnerDocumentDwollaV1HalJSONError
    | DwollaError
    | ResponseValidationError
    | ConnectionError
    | RequestAbortedError
    | RequestTimeoutError
    | InvalidRequestError
    | UnexpectedClientError
    | SDKValidationError
  >(
    M.nil(
      201,
      operations.CreateBeneficialOwnerDocumentResponse$inboundSchema.optional(),
      { hdrs: true },
    ),
    M.jsonErr(
      400,
      errors.CreateBeneficialOwnerDocumentDwollaV1HalJSON$inboundSchema,
      { ctype: "application/vnd.dwolla.v1.hal+json" },
    ),
    M.jsonErr(403, errors.ForbiddenError$inboundSchema, {
      ctype: "application/vnd.dwolla.v1.hal+json",
    }),
    M.jsonErr(404, errors.NotFoundError$inboundSchema, {
      ctype: "application/vnd.dwolla.v1.hal+json",
    }),
    M.jsonErr(
      413,
      errors.CreateBeneficialOwnerDocumentDwollaV1HalJSONError$inboundSchema,
      { ctype: "application/vnd.dwolla.v1.hal+json" },
    ),
    M.fail("4XX"),
    M.fail("5XX"),
  )(response, req, { extraFields: responseFields });
  if (!result.ok) {
    return [result, { status: "complete", request: req, response }];
  }

  return [result, { status: "complete", request: req, response }];
}
